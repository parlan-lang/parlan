extern func printf(fmt: str, ...): int;

func fibonacci(n: int):int {
    if n < 2 {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

func main(): int {
    var f20: int = fibonacci(20);
    printf("fibonacci of 20 = %d", f20);
    return 0;
}